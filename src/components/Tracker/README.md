# Tracker — Code Connect

Maps Figma `tracker` (`2054:4704`) → `Tracker`.

| Figma | Code |
|-------|------|
| `count` | `step` |
| `step name` | `label` |
| `State` | `status` (`default` \| `active` \| `completed`) |
| `branch` | `showConnector` (false when unchecked) |
