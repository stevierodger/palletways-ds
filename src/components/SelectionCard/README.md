# SelectionCard

Economy / premium option card from Figma `selection-cards` (`2069:5436`). Reuses `Button`, `ArrowCircleRight`, `ChevronRight`. Hover via CSS; `selected` and `empty` are explicit props.

## Deploy

Code Connect source: GitHub `main` `src/components/SelectionCard/SelectionCard.tsx`.
