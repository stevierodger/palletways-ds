# DateCard

Collection date picker from Figma `date-card` (`2296:14613`). Composes `CalendarDate`, `ChevronLeft`, `ChevronRight`.

## Deploy

Code Connect source: GitHub `main` `src/components/DateCard/DateCard.tsx`.
