# HeaderNavigation

Site header bar from Figma `header-navigation` (`194:2378`). Composes `LogoWordmark` + `NavMenuItems`.

| State | Logo | Nav |
|-------|------|-----|
| `desktop` | large | expanded |
| `desktop-cta` | small | expanded-cta |
| `mobile` | small | collapsed |

## CSS variables

- `--white` — background
- `--brand-green-800` — bottom border (#1f4400)
- `--spacing-4`, `--spacing-6` — padding (16px 24px)

## Deploy

Code Connect source: GitHub `main` `src/components/HeaderNavigation/HeaderNavigation.tsx`.
