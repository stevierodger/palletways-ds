# SummaryFooterActionPanel

Summary footer action row: ghost terms link + secondary login button. Figma `summary-footer/action-panel` (`2287:10610`).

Composes `Button` (`ghost` sm + `secondary` sm).

## Deploy

Code Connect source: GitHub `main` `src/components/SummaryFooterActionPanel/SummaryFooterActionPanel.tsx`.
