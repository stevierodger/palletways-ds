# CalendarDate

Day cell from Figma `calendar-date` (`2059:4796`).

| Figma State | Code |
|-------------|------|
| default / default-hover | `tone="default"` (+ CSS hover) |
| empty / empty-hover | `tone="empty"` |
| today / today-hover | `tone="today"` |
| collection-* | `tone="collection"` |
| travel-* | `tone="travel"` |
| delivery-* | `tone="delivery"` |
| disabled | `disabled` |

## Deploy

Code Connect source: GitHub `main` `src/components/CalendarDate/CalendarDate.tsx`.
