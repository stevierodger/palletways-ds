# Tag — Code Connect

No dedicated Tag component set in Figma. Mapped published `label` chip (`2283:9436`) → `Tag`.

| Figma | Code |
|-------|------|
| `info-label` | `label` |
| (none) | `tone` defaults to `neutral` — create Figma variants when tones are designed |
