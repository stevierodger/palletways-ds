# PalletSizeInfo

Dimension row from Figma `info.pallet-size` (`2007:4430`). `changed` maps to State=change (asterisk).

## Deploy

Code Connect source: GitHub `main` `src/components/PalletSizeInfo/PalletSizeInfo.tsx`.
