# Breadcrumbs

Text trail from Figma `Breadcrumbs` (`194:2939`). Current segment uses Median (600); ancestors Neutral (400); separator `/`.

## Behaviour

- Path links have **no** underline by default; underline appears on hover only (overrides design-system global `a` styles).
- Separators, links, and current page share one baseline (`align-items: baseline` + shared font metrics).

## Deploy

Code Connect source: GitHub `main` `src/components/Breadcrumbs/Breadcrumbs.tsx`.
