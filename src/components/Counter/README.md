# Counter

Quantity stepper from Figma `action.counter` (`2006:4000`). Minus / value / plus using `RemoveMinus` + `AddPlus` icons (24px in 20px clip slots). Display pads to 2 digits by default (`01`).

## Files

| Path | Role |
|------|------|
| `Counter.tsx` / `Counter.css` | Component |
| `counter.figma.ts` | Code Connect |
| `index.ts` | Exports |

## Tokens

`--surface-color-bg-natural-default`, `--border-color-border-bold-default`, `--text-color-text-body-secondary-default`, `--typography-body-strong-*`, `--spacing-comp-*-sm`

## Deploy

Code Connect source: GitHub `main` `src/components/Counter/Counter.tsx`.
